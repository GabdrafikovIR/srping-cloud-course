server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: git
  cloud:
    config:
      discovery:
        enabled: true
      server:
        git:
          uri:
          search-paths:
          default-label: master
          clone-on-start: true